		<div class="inner clear" id="list">
			<% @links.each do |link| %>
				<div class="Link">
					<h2><%= link_to link.title, link.url %></h2>
					<p><%= link.description %></p>
				</div>
			<% end %>
		</div>