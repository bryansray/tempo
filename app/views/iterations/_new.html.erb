<% form_remote_for( [team, Iteration.new], :html => {:id => "iteration_form"} ) do |f| %>
	<%= render :partial => "iterations/form", :locals => { :f => f, :started => team.next_available_startdate, :ended => team.next_available_startdate + 12 } %>
	<%= f.submit "Create" %>
<% end %>